<section class="contact-section1 ">
    <div class="container">
        <h2 class="text-center mb-4">Kontaktirajte nas</h2>
        <form asp-action="Contact" method="post" enctype="multipart/form-data" class="mx-auto" style="max-width:600px;">
            <div class="mb-3">
                <label for="Name" class="form-label">Ime i prezime</label>
                <input type="text" class="form-control" id="Name" name="Name" required>
            </div>
            <div class="mb-3">
                <label for="Email" class="form-label">Email</label>
                <input type="email" class="form-control" id="Email" name="Email" required>
            </div>
            <div class="mb-3">
                <label for="Message" class="form-label">Poruka</label>
                <textarea class="form-control" id="Message" name="Message" rows="4" required></textarea>
            </div>
            <div class="mb-3">
                <label for="CV" class="form-label">Upload CV (opciono)</label>
                <input type="file" class="form-control" id="CV" name="CV">
            </div>
            <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#successModal">
                Pošalji poruku
            </button>
        </form>
    </div>
</section>



<div class="modal fade" id="successModal" tabindex="-1" aria-labelledby="successModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 rounded-4 shadow-lg">
            <div class="modal-header border-0 bg-primary text-white rounded-top-4 py-3">
                <h5 class="modal-title fw-bold" id="successModalLabel">Prijava primljena</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body text-center p-5">
                <div class="display-1 text-primary mb-3">
                    <i class="bi bi-file-earmark-check"></i>
                </div>

                <h3 class="fw-bold text-dark">Uspešna prijava!</h3>

                <div class="mt-3">
                    <p class="text-muted">
                        Vaša prijava za posao je <strong>uspešno evidentirana</strong> u našoj bazi kandidata.
                    </p>
                    <p class="small text-secondary">
                        Naš HR tim će pažljivo pregledati Vaš CV. Ukoliko Vaš profil odgovara našim trenutnim potrebama, kontaktiraćemo Vas u najkraćem roku.
                    </p>
                </div>
            </div>

            <div class="modal-footer border-0 pb-4 justify-content-center">
                <button type="button"
                        class="btn btn-primary rounded-pill px-5 fw-bold shadow-sm"
                        data-bs-dismiss="modal"
                        onclick="location.reload()">
                    ZATVORI
                </button>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <script>
        $(document).ready(function () {
            $('#successModal .btn').on('click', function () {
                location.reload();
            });
        });
    </script>
}